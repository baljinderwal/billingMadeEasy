{
  "name": "@billing/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for billingMadeEasy platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "mongoose": "^8.0.3"
  }
}
